<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.su.sakuraanimeplugin">

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_sakura"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sakura_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaBoxPluginSample">
        <activity
            android:name=".OtherActivity"
            android:exported="true"
            android:label="关于">
            <intent-filter>
                <!--这个Action只能标一次，一般可以标记在写关于信息的Activity上-->
                <action android:name="media_box_plugin_action" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>